---
name: springboot2-postgresql-mvnw
version: '1.0'
description: Backend project that uses Maven, Java 8, Spring Boot 2
tags:
  - backend
  - maven
  - java
  - springboot

values:
  schema: ./parameters-schema.json
  defaults: ./parameters-defaults.yaml

generator:
  image: mustache-cli:local
  args:
    - --exclude=.*jar

