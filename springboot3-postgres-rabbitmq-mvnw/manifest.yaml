---
name: Maven, Java 8, Spring Boot
version: '1.0'
description: Backend project that uses Maven, Java 8, Spring Boot 3, PostgreSQL, RabbitMQ
tags: [ 'backend', 'maven', 'java', 'springboot' ]

values:
  schema: ./parameters-schema.json
  defaults: ./parameters-defaults.yaml

engine:
  image: mustache-cli:local
  args:
    - --exclude=.*jar

