---
name: springboot2-hex-mvnw
version: '1.0'
description: Multimodule template for HEXagonal springboot 2 app with REST & JPA
tags: [ 'backend', 'maven', 'java', 'springboot', 'hexagonal', 'multimodule' ]

values:
  schema: ./parameters-schema.json
  defaults: ./parameters-defaults.yaml

generator:
  image: mustache-cli:local
  args:
    - --exclude=.*jar

